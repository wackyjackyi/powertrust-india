# PowerTrust India — dependencies
# Install with: pip install -r requirements.txt

# Layer 1 — ingestion
requests==2.32.3
beautifulsoup4==4.12.3
pypdf==4.3.1
lxml==5.2.2           # faster HTML parser for BeautifulSoup

# Layer 2/3 — embeddings + vector store
sentence-transformers==3.0.1
faiss-cpu==1.8.0
numpy==1.26.4

# Layer 4 — LLM
groq==0.9.0

# Layer 5 — UI
streamlit==1.36.0
plotly==5.22.0
pandas==2.2.2

# Utilities
python-dotenv==1.0.1  # for storing GROQ_API_KEY in .env
tqdm==4.66.4

# RAG chat
chromadb==0.5.0
httpx>=0.27.0
